<div class="w-full">
  <div class="mb-6 flex items-center justify-between">
    <h1 class="font1 text-4xl font-bold text-gray-900 dark:text-zinc-100"><%= t("users.index.title") %></h1>
  </div>

  <%= render "list", users: @users %>
</div>
