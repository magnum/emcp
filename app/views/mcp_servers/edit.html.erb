<% content_for :title, "Edit #{@server.name}" %>

<div class="w-full max-w-2xl mx-auto">
  <p class="mb-4">
    <%= link_to "← #{@server.name}", mcp_server_path(@server), class: "font-semibold underline" %>
  </p>
  <h1 class="mb-8 text-3xl font-bold tracking-tight">Edit server</h1>
  <%= render "form", server: @server, server_types: @server_types %>
</div>
